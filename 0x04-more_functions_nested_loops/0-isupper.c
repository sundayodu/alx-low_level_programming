#include "main.h"

/**
  * _isupper - prints the alphabets in upper case
  *
  * @c: parameter to be printed
  *
  * Return: 1 if c is upper
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	return (1);
	else
	return (0);
}
